# Optional overrides for environment YAML variables.
# meowfacts uses inline URLs in environment YAML — no .env entry needed.
# Add overrides here only for private APIs or staging domains.

# httpbin (default in env YAML: https://httpbin.org)
HTTPBIN_BASE_URL=https://httpbin.org

# Server
PORT=3001
