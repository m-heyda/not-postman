import { lazy } from "react";

/** Lazy Monaco stub — wired in a future plan for JSON body editing. */
export const MonacoEditorStub = lazy(async () => ({
  default: function MonacoEditorStub() {
    return null;
  },
}));
